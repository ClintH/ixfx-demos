{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against browser-sync",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}