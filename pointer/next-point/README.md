# next-point

This sketch shows the current position of the pointer as well as the predicted next point. The prediction is extremely simple: it uses the vector (angle/distance) of averaged movement. As such it doesn't take into account curved motion.